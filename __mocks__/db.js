// when you see this file again, ask teachers, what it was meant for ..I think
// it should mock the database queries and return whatever you whish for

// jest.mock('./')

// SIGN ADD /////////////////////////
// module.exports.addSign = jest.fn().mockResolvedValue(43);
